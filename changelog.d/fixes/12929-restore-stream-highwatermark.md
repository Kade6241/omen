- **fix(stream):** Restore the per-stream `highWaterMark` plumbing from #12179 that #12506 accidentally dropped while hardening the public error boundaries — `open-sse/executors/glm.ts` still passes the 64KB buffer argument, so the API-route typecheck gate has been failing on `release/v3.8.51` (TS2554, 16 args vs. 15) and GLM streams silently fell back to the default 16KB buffer. `StreamOptions.highWaterMark` and the trailing optional parameter on `createSSETransformStreamWithLogger` / `createPassthroughStreamWithLogger` are back; default behavior is unchanged (16384)
